db_path: db/jobs.db
http_timeout_sec: 15
http_retries: 2

verify:
  limit_default: 100
  per_company_pause_sec: 0.10
  max_search_results: 8
  log_every: 20
  max_discovery_minutes: 8
  max_workers: 3

track:
  mark_closed_after_days_not_seen: 10

export:
  new_window_hours: 24
